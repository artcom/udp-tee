<h1>Listing joints</h1>

<table>
  <tr>
    <th>Interface</th>
    <th>Port</th>
    <th>Open</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @joints.each do |joint| %>
  <tr>
    <td><%= joint.interface %></td>
    <td><%= joint.port %></td>
    <td><%= joint.open %></td>
    <td><%= link_to 'Show', joint %></td>
    <td><%= link_to 'Edit', edit_joint_path(joint) %></td>
    <td><%= link_to 'Destroy', joint, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Joint', new_joint_path %>
